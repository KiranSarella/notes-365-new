#  Timeline Detail

single or array of dates -
load each day and ask for next day.. repeat till all are loaded.
if that day don't have any data? try to load next day.

if all days are completed and all are empty. then show empty message, till then show loader.



